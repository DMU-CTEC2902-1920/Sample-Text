@model dmuBlogger.Models.Blacklist

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Blacklist</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.BlacklistIP)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.BlacklistIP)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.BlacklistReason)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.BlacklistReason)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.BlacklistId }) |
    @Html.ActionLink("Remove from blacklist", "Delete", new { id = Model.BlacklistId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
